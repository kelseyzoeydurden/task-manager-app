# task-manager-app
A program that automatically loads tasks from a file, allows the user to input new tasks, save tasks to the file, mark a task as complete, edit a task, delete a task, clear all tasks, and save all tasks to update the file. 
